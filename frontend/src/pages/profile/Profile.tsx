import styles from "./_Profile.module.scss";

const Profile = () => {
    return (
        <div className={styles.container}>
            <h1>Profile</h1>
        </div>
    );
};

export default Profile;